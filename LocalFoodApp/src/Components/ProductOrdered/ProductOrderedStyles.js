import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  containerProduct: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'flex-start',
    marginBottom: 10,
  },
  productImage: {
    width: 50,
    height: 50,
    borderRadius: 10,
    marginRight: 15,
  }
})